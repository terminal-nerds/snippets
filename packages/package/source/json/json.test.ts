import { returns, throws } from "@terminal-nerds/snippets-test/unit";
import { describe, it } from "vitest";

import {} from "./json.ts";

describe(``, () => {
	it(``, ({ expect }) => {
		expect();
	});
});

---
"@terminal-nerds/snippets-string": minor
---

✨ Added a module `word` for grouped snippets. It includes:

-   `hasLatinChars()`
-   `hasNumberChars()`
-   `hasSpecialChars()`
-   `reverseString()`
-   `isPalindrome()`
-   `isNumeric()`
